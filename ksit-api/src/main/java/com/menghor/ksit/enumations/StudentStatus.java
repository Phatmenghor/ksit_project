package com.menghor.ksit.enumations;

public enum StudentStatus {
    STUDYING,  // Student is currently studying
    GRADUATED, // Student has graduated
    DROPPED_OUT, // Student has dropped out
    SUSPENDED, // Student is suspended
    TRANSFERRED // Student has transferred to another institution
}
