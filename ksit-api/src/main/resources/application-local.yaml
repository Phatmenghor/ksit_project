spring:
  datasource:
    url: jdbc:postgresql://localhost:1111/ks_it_school
    username: postgres
    password: Hour1819
    driver-class-name: org.postgresql.Driver
    hikari:
      maximum-pool-size: 10
      minimum-idle: 2
      connection-timeout: 30000
      idle-timeout: 300000
      max-lifetime: 1800000

  jpa:
    show-sql: true

logging:
  level:
    com.menghor.ksit: DEBUG
    org.hibernate.SQL: DEBUG
